import app from "@react-native-firebase/app";
import auth from "@react-native-firebase/auth";

// If we add support for web in the future, we need to modify this file
// and setup initialization for web as well.

export { app, auth };
